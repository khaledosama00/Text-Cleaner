Port Scanner is a simple Python script that checks whether a specific TCP port is open or closed on a given IP address or website.
It uses the built-in socket library to attempt a TCP connection and determine the port status based on the connection result.

🚀 Features

Scan a specific TCP port

Accepts both IP addresses and domain names

Detects whether the port is OPEN or CLOSED

Uses socket timeout to avoid long waits

Simple and easy-to-use command-line interface

🛠 Technologies Used

Python

socket module

TCP connection
